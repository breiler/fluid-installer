export enum Page {
    HOME = "/",
    TERMINAL = "/terminal",
    INSTALLER = "/install",
    FILEBROWSER = "/files",
    CONFIGURATION = "/configuration",
    WIFI = "/wifi",
    CALIBRATE = "/calibrate"
}

export default Page;
